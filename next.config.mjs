/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.freepik.com', 'encrypted-tbn0.gstatic.com'], // Add the domains here
    },
  };
  
  export default nextConfig;
  